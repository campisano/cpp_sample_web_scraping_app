language: cpp
compiler: gcc
os: linux
dist: trusty
addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - make
            - cmake
            - g++-6
            - libcurl4-openssl-dev
            - libpq-dev
script:
    - export CXX="g++-6"
    - make -e
