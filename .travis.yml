language: cpp
compiler: gcc
os: linux
dist: trusty
addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - astyle
            - make
            - cmake
            - g++-6
script:
    - export CXX="g++-6"
    - make -e
